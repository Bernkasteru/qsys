worker_processes auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 4096;
    multi_accept on;
}

http {
    limit_req_zone $http_x_real_ip zone=ip_limit:10m rate=50r/s;
    limit_req_zone $server_name zone=global_limit:100m rate=6200r/s;

    upstream qsys_backend {
        # Go HTTP svr port
        # 默认轮询 (Round robin)
        # or:
        # ip_hash
        server qsys-cli-1:8080; 
        server qsys-cli-2:8080;
    }

    server {
        listen 80;
        server_name qsys_gateway;

        location / {
            limit_req zone=ip_limit nodelay;
            limit_req zone=global_limit burst=60 nodelay;
            proxy_pass http://qsys_backend;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}